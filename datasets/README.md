# Datasets

Dataset manifests, provenance notes, and lightweight metadata belong here.

Large raw and processed datasets are intentionally ignored by Git and should be managed through object storage or a dedicated data-versioning workflow.
