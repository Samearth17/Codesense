# Models

Model cards, registry metadata, and promotion notes belong here.

Large binary model artifacts are intentionally ignored by Git and should be managed through a model registry or object storage.
